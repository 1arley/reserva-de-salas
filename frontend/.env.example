# URL base da API NestJS (backend). O backend já libera CORS para http://localhost:4200.
NEXT_PUBLIC_API_URL=http://localhost:3000
# Prefixo global das rotas da API (espelha API_PREFIX do backend).
NEXT_PUBLIC_API_PREFIX=/api
